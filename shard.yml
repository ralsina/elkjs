name: elkjs
version: 0.1.0

scripts:
  postinstall: cd ../src && gcc -DJS_DUMP -c  elk.c -o elk.o

authors:
  - Roberto Alsina <roberto.alsina@gmail.com>

crystal: '>= 1.12.2'

license: MIT

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
